#include "main.h"

/**
 * _abs - Computes absolute value of an integer
 * @n: The int for check
 * Return: The absolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
