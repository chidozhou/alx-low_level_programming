#include "main.h"
/**
 * _isupper - Checks for the uppercase characters
 * @c: This is the variable txt
 * Return: Always 0.
*/
int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
{
return (1);
}
else
{
return (0);
}
}
